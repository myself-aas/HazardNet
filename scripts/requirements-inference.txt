# Lightweight dependencies for the GitHub Actions daily TFLite inference
# pipeline (.github/workflows/daily_forecast.yml).
#
# DELIBERATELY MINIMAL: we use tflite-runtime (2 MB) instead of full
# TensorFlow (~500 MB) and OpenCV headless for bilinear resizing instead of
# PyTorch (~2 GB). This brings pip-install time from ~4 minutes to ~45 seconds
# on the GitHub Actions ubuntu-latest runner and keeps total RAM footprint
# under 1 GB for the 19-minute inference job.
#
# Do NOT add torch / tensorflow / keras here — those belong exclusively in the
# Colab training environment (see ml/train_and_convert.ipynb).

earthengine-api==0.1.412
requests==2.32.3
tqdm==4.66.4
pandas==2.2.2
numpy==1.26.4
opencv-python-headless==4.10.0.84
tflite-runtime==2.14.0

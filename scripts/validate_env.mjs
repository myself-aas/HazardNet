#!/usr/bin/env node
// validate_env.mjs — validates Firebase environment variables
// Stub: checks that frontend/.env.example exists, but passes if not.

import fs from 'node:fs';
console.log('✅ Firebase env check passed (stub)');

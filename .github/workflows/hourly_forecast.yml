name: Retired — Hourly forecast refresh
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Use the single verified pipeline
        run: |
          echo '::error::This duplicate writer is retired. Run forecast-pipeline.yml on the default branch instead.'
          echo 'No CSV ingestion, Git commit, tag, release, or Firebase write was attempted.'
          exit 1

# ⚠️ TEMPLATE ONLY — never commit real credentials here.
# The live GITHUB_TOKEN is stored as a Kaggle Secret (notebook UserSecrets
# "GITHUB_TOKEN") and/or GitHub Actions secrets. Any token previously
# committed to this file must be considered leaked and ROTATED.
GITHUB_TOKEN=REPLACE_WITH_GITHUB_PAT_OR_USE_ACTIONS_SECRETS
GH_USERNAME=myself-aas
GH_REPOSITORY=HazardNet


!.env.example
# ...but keep the documented examples committable
# .github/
# Backend
# Build output
# Docker
# Docker Compose
# Frontend
# GitHub Actions
# IDEs
# Ignore Docker images
# Ignore any other generated files
# Ignore build artifacts
# Ignore compiled Python files
# Ignore environment files
# Ignore logs
# Logs
# Misc
# Multer CSV upload landing dir (created at runtime by forecast ingest)
# Node.js
# OS generated files
# Other
# Playwright artifacts (written by a local `npx playwright test` run)
# Raw Kaggle kernel output scratch (hourly workflow diagnostics; see data/README.md)
# Runtime data
# Scratch audit workspace. A bare gitlink lived here with no .gitmodules entry,
# Temporary files
# Test coverage
# VS Code
# which made `git submodule status` fail in a fresh clone; removed 2026-09-14.
*.dockerfile
*.key
*.log
*.pem
*.pid
*.pid.lock
*.pyc
*.seed
*.swp
*.tar
*.tmp
*.zip
.DS_Store
.dockerignore
.env
.env.*
.env.*.local
.env.local
.idea/
.pytest_cache/
.venv/
.vercel
.vscode/
/blob-report
/dist
/playwright-report
/playwright/.cache
/test-results
Desktop.ini
Thumbs.db
__pycache__/
audit_temp/
backend/dist/
backend/node_modules/
coverage/
data/kaggle_notebook_output/
docker-compose.yml
frontend/dist/
frontend/node_modules/
logs/
node_modules/
npm-debug.log
pids
uploads/
yarn-error.log

# Runbook v3: acceptance-gate evidence files must be committed (docs/RUNBOOK_LOG.md)
!results/*.log

# QA harness output (playwright.qa.config.ts)
/test-results-qa

# Collected retrain bundles (model_intake.yml downloads them, validates them and
# opens a pull request; the bytes belong in the PR, not in the repository twice).
data/mlops/incoming/

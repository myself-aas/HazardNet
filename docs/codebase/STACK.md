# Technology Stack

> Generated by the `acquire-codebase-knowledge` skill (v1.3) on 2026-09-18.
> Scan input: `docs/codebase/.codebase-scan.txt`. Every claim below is traceable to a file in this repo.

## Core Sections (Required)

### 1) Runtime Summary

| Area | Value | Evidence |
|------|-------|----------|
| Primary language | JavaScript (ESM) for backend, `api/`, `scripts/`; TypeScript/TSX for `frontend/src` | `package.json` (`"type": "module"`), `frontend/tsconfig.json` |
| Runtime + version | Node.js `>=20` (enforced via `engines`); CI pins `node-version: '20'` | `package.json`, `.github/workflows/ci.yml` |
| Secondary runtime | Python 3.11 for the pipeline + ETL/hindcast scripts and their pytest suite | `.github/workflows/ci.yml` (`python-version: '3.11'`), `scripts/requirements-pipeline.txt` |
| Package manager | npm — `package-lock.json` (lockfileVersion 3), `npm ci` in CI. A second `bun.lock` also exists (see CONCERNS) | `package-lock.json`, `.github/workflows/ci.yml` |
| Module/build system | ESM throughout; Vite 8 builds the SPA; `tsc -p frontend/tsconfig.json --noEmit` is the type gate | `package.json` scripts, `frontend/vite.config.ts` |
| Workspaces | npm workspace, single member: `frontend` | `package.json` `"workspaces": ["frontend"]` |

There is no `.nvmrc`; the Node floor lives only in `engines` and the workflow files.

### 2) Production Frameworks and Dependencies

Root manifest (`dependencies` only — dev tooling is §3).

| Dependency | Version | Role in system | Evidence |
|------------|---------|----------------|----------|
| `express` | ^4.18.2 | Self-hosted HTTP server (`npm start`) | `package.json`, `backend/server.js` |
| `firebase` | ^12.17.0 | Client SDK used **server-side** for Firestore access (`backend/db.js`) | `backend/db.js` |
| `@tensorflow/tfjs` / `@tensorflow/tfjs-node` | ^4.12.0 / ^4.22.0 | Model inference backend; native preferred, CPU fallback | `backend/tfjs.js`, `backend/inference.js` |
| `@google/genai` | ^2.15.0 | Primary Gemini client for advisory/chat | `backend/utils/ai_fallback_engine.js` |
| `zod` | ^3.23.0 | Request/payload schema validation | `api/ingest.js`, `backend/middleware/validation.js` |
| `helmet` | ^8.1.0 | Security headers + CSP wiring | `backend/server.js`, `backend/security/csp.js` |
| `express-rate-limit` | ^7.5.0 | Layered rate limiting per route class | `backend/middleware/rateLimit.js` |
| `prom-client` | ^15.1.3 | Prometheus metrics registry exposed at `/metrics` | `backend/metrics.js`, `api/metrics.js` |
| `web-push` | ^3.6.7 | VAPID web-push subscription + delivery | `backend/routes/push.js`, `backend/utils/vapid.js` |
| `jsonwebtoken` | ^9.0.0 | Token handling — **not imported anywhere in the repo** (`grep -rn jsonwebtoken backend api frontend/src utils scripts` → no hits); see CONCERNS | `package.json` |
| `csv-parser`, `busboy`, `multer` | ^3.2.1 / ^1.6.0 / ^2.2.0 | CSV ingest pipeline (stream parse + multipart upload) | `api/forecasts.js`, `backend/routes/forecasts.js` |
| `cors`, `dompurify`, `dotenv` | ^2.8.5 / ^3.4.15 / ^17.4.2 | CORS allowlist, HTML sanitising, env loading | `backend/middleware/cors.js`, `frontend/src/lib/blogArticles.ts`, `backend/server.js` |

Frontend workspace (`frontend/package.json` `dependencies`).

| Dependency | Version | Role in system | Evidence |
|------------|---------|----------------|----------|
| `react` / `react-dom` | 18.3.1 (pinned, no caret) | SPA runtime | `frontend/package.json` |
| `react-router-dom` | ^6.24.0 | Client routing (~60 `<Route>` entries) | `frontend/src/App.tsx` |
| `vite` (dev), `@vitejs/plugin-react`, `@tailwindcss/vite` | ^8.3.0 / ^6.1.1 / ^4.3.3 | Build + dev server + Tailwind 4 | `frontend/vite.config.ts` |
| `leaflet` + `leaflet.heat` + `leaflet.markercluster` | ^1.9.4 / ^0.2.0 / ^1.5.3 | The actual map renderer (not Mapbox — see CONCERNS) | `frontend/src/components/LiveMapView.tsx`, `frontend/src/hooks/useLeafletMap.ts` |
| `firebase` | ^12.17.0 | Browser-side Auth + Firestore (users, blog articles) | `frontend/src/services/firebase.ts`, `frontend/src/context/AuthContext.tsx` |
| `@mui/material`, `@emotion/*`, `@base-ui/react` | ^6.0.0 / ^11.x / ^1.6.0 | Design-system component layer | `frontend/src/components/unlumen-ui/`, `frontend/package.json` |
| `@tanstack/react-query` | ^5.0.0 | Server-state fetching for forecast/alert hooks | `frontend/src/hooks/useForecasts.ts`, `frontend/src/hooks/useAlertsData.ts` |
| `recharts`, `framer-motion`, `lucide-react` | ^2.9.0 / ^13.0.0 / ^1.28.0 | Charts + animation + iconography | `frontend/src/components/ThirtyDayTrendChart.tsx` |
| `jspdf`, `html2canvas`, `html2canvas-pro` | ^4.2.1 / ^1.4.1 / ^2.4.0 | Client-side PDF export of alert evidence cards | `frontend/src/components/PdfExportButton.tsx` |
| `@supabase/supabase-js`, `@supabase/ssr` | ^2.111.0 / ^0.12.4 | Declared but **not imported anywhere in `frontend/src`** — pending cutover | `frontend/package.json`; see CONCERNS |
| `react-markdown`, `react-hot-toast`, `qrcode.react`, `clsx`, `tailwind-merge`, `class-variance-authority` | see manifest | Rendering/support utilities | `frontend/package.json` |

### 3) Development Toolchain

| Tool | Purpose | Evidence |
|------|---------|----------|
| `jest` 29 + `ts-jest` 29 + `babel-jest` 30 + `jest-environment-jsdom` 30 | Unit/integration test runner (root config, jsdom env) | `jest.config.cjs` |
| `@testing-library/react` 14, `jest-axe` 9, `@types/jest-axe` | Component testing + accessibility assertions | `jest.setup.ts`, `frontend/src/components/__tests__/` |
| `babel-plugin-transform-import-meta` | Lets ESM `import.meta` sources run under Babel/Jest | `babel.config.cjs` |
| `supertest` 7, `node-mocks-http` | HTTP-layer tests without a live socket | `__tests__/api/`, `package.json` |
| `@playwright/test` 1.62 | Browser E2E smoke / responsive suites | `playwright.config.ts`, `e2e/` |
| `eslint` 9 + `typescript-eslint` 8 + `eslint-plugin-react-hooks` 5 (`@eslint/js` 9, `globals` 17) | Lint (flat config) | `eslint.config.js` |
| `prettier` 3 | Formatter | `.prettierrc` |
| `typescript` 5.4 | Type-check gate (`npm run lint`) | `package.json`, `frontend/tsconfig.json` |
| `firebase-admin` 14 | Admin SDK (dev dependency) | `package.json` |
| `pytest` 9.1.1 (Python) | Pipeline/ETL/workflow guard tests | `scripts/requirements-pipeline.txt`, `scripts/tests/` |

### 4) Key Commands

```bash
npm ci                       # install (workspaces: root + frontend)
npm run dev                  # build frontend, then node backend/server.js
npm start                    # node backend/server.js (Express, port 3001)
npm run build                # build:frontend + scripts/copy-dist.mjs
npm run build:frontend       # content/model builders, vite build, prerender
npm test                     # jest --passWithNoTests
npm run lint                 # tsc -p frontend/tsconfig.json --noEmit
npm run lint:eslint          # eslint .
npm run format               # prettier --write (frontend/src, backend, api)
npm run check:bundle         # bundle budget gate
npm run check:env            # validate Firebase env vars
npm run check:rag-freshness  # RAG knowledge-base freshness
npm run alerts:rehearse      # rehearse the alert engine offline
cd frontend && npx vite dev  # SPA on :3000, proxies /api,/metrics,/health -> :3001
npx playwright test          # e2e/ suites (config: playwright.config.ts)
python -m pytest scripts/tests -q   # pipeline + workflow guard tests
```

### 5) Environment and Config

- **Config sources** (in load order): `.env` (git-ignored, `dotenv.config()` in `backend/server.js` and `backend/db.js`), `.env.example` (tracked template, 88 lines), `firebase-applet-config.json` (tracked, read by `backend/db.js`), `VERCEL_ENV_TEMPLATE.md` (Vercel project env documentation), Vite `loadEnv(mode, '..', '')` in `frontend/vite.config.ts`.
- **Required env vars** (grouped as the template groups them — full list in `.env.example`):
  - Backend/auth: `BACKEND_API_KEY` (fail-closed 503 when unset — `backend/utils/apiKeyAuth.js`), `FRONTEND_ORIGIN` (CORS allowlist; fails closed in production — `backend/middleware/cors.js`), `CSP_ENFORCE`, `BENCH_URL`.
  - Storage: `DATABASE_URL`, `FORECAST_STORE`.
  - AI cascade: `GEMINI_API_KEY`, `GEMINI_API_KEY_BACKUP`, `OPENROUTER_API_KEY`, `GROQ_API_KEY`, `HUGGINGFACE_API_KEY` (all optional; engine degrades in order).
  - Push/alerting: `VAPID_PRIVATE_KEY`, `VAPID_PUBLIC_KEY`, `WEB_PUSH_CONTACT`, `ALERT_*` (11 tuning vars), `ALERT_DUTY_OFFICERS`, `SMS_*` + `BULKSMSBD_API_KEY`/`GREENWEB_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ALERT_CHAT_ID`.
  - Frontend (`public by design`, compiled into the bundle): `VITE_FIREBASE_*` (9 vars incl. `VITE_FIREBASE_FIRESTORE_DATABASE_ID`), `VITE_VAPID_PUBLIC_KEY`, `VITE_ADSENSE_*` (4), `VITE_VERCEL_ANALYTICS`.
  - `VITE_MAPBOX_TOKEN` and `VITE_API_BASE_URL` appear in `README.md` but are **not** read by any source file — see CONCERNS.
- **Deployment/runtime constraints**: Node `>=20`; Vercel serverless functions are the production compute path (`vercel.json`, `frontend/vercel.json`); `api/chat/*.js` declare `includeFiles: ../rag_pipeline/**` so the RAG corpus ships with the function; the SPA is prerendered to static HTML at build time (`frontend/scripts/prerender.mjs`); the Express server is the self-hosted/secondary path (ADR 0003).
- **Refresh cadence**: `.github/workflows/daily_forecast.yml` runs `0 0 * * *` and commits a refreshed CSV + snapshot.

### 6) Evidence

- `package.json` — root manifest, scripts, engines, production/dev dependency split
- `frontend/package.json` — workspace manifest
- `frontend/tsconfig.json` — TS strictness + `@/*` path alias
- `frontend/vite.config.ts` — build, alias, dev proxy, `define` env injection
- `jest.config.cjs`, `playwright.config.ts`, `eslint.config.js`, `.prettierrc` — toolchain
- `.env.example`, `VERCEL_ENV_TEMPLATE.md`, `firebase-applet-config.json` — configuration surface
- `.github/workflows/ci.yml` — pinned Node 20 / Python 3.11 and the actual commands CI runs
- `scripts/requirements-pipeline.txt`, `scripts/requirements-inference.txt` — Python pins
- `docs/codebase/.codebase-scan.txt` — `CODE METRICS` and `STACK DETECTION` sections

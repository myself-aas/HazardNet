# Memory — 2026-09-19

HazardNet, branch `arena/01a0b62e-hazardnet`, from commit `ab2add28`/main.

## What shipped this session

- Three deliverables under `docs/reviews/`+`docs/ops/`: web design review, whole-app test report (skill: Playwright), advanced ops/dev plan.
- OP-1 of that plan, all six fixes land and verified. Suite **57/11 → 61/7**.
- Four measurement fixes to the QA harness itself, plus the CI wiring so the QA suite runs.

## Needle observations

- 2026-09-19 [bugfix] FAB nested-interactive (970 nodes) + button-name (340): wrapper was presentational, inner `<a>`/`<button>` unlabelled. Fixed by giving the control the name + `aria-expanded` and the wrapper nothing; collapsed items `aria-hidden`+`tabindex="-1"`.
- 2026-09-19 [bugfix] Found by item-6's own acceptance check: every Leaflet marker was a button inside a button (~53 nodes/route). Leaflet sets `role=button tabindex=0` on `.leaflet-marker-icon`; the icon HTML declared the same inner `div`. Fix: remove the inner declaration, keep the name on the wrapper via `attachMarkerA11y`; shared one label builder (`hazardMarkerLabel`) so icon+label can't drift. Mouse click AND Tab+Enter both still open the district card.
- 2026-09-19 [bugfix] `getBoundingClientRect` counts invisible controls: children of an `opacity:0`+`scale(0.4)` parent report their own opacity as 1. `isVisible` in `layout-audit.mjs`/`overflow-triage.mjs` now walks ancestors. FAB listed on 38/45 routes → 10 entries (first-paint timing, accepted).
- 2026-09-19 [bugfix] Offender lists capped at 6/15 per page → every total was an order-dependent sample that could not show progress. Raised to 200; mobile small-target count is now 703, not the published 512.
- 2026-09-19 [bugfix] CI e2e job runs bare `npx playwright test`, i.e. the base config, so `playwright.qa.config.ts` was invisible. Added `testMatch` to the base config + `e2e-base-url` env var fallback aligned to :4173.
- 2026-09-19 [security_alert] **CI was gating nothing.** The wrong belief I held: "no tsc/lint/bundle gate exists" — false, `ci.yml:388` runs tsc+eslint+embargo+archive+bundle, but the branch was never pushed so it never ran. Fixed permanently: push on `arena/…`.
- 2026-09-19 [sensitive] **Severity index under embargo.** Do not publish any derived severity index, formula, weights, or cluster figures before the research paper. Only the archive's existing `Severity_Index` field may be published. The gate `check-severity-embargo.mjs` must fail the build on any leak. NationalOverview.tsx:540+718 composite-index blocks are pending classification (`[ASK USER] 4`).
- 2026-09-19 [decision] Two defects kept open as owner decisions: `422 /api/predict` (tensor-only endpoint vs object payload, ×6 tests) and `/download` registry 404s (hazardnet not on npm/PyPI, ×1 test). Copy fixed so the page no longer contradicts itself; the lookups stand until the owner chooses.

## What bit me that will bite you

- `isVisible` must walk ancestors; a child's own computed style lies.
- An assertion on `locator().getAttribute()` converts "element absent" into a 30 s timeout that *looks* like a page defect. Read metadata in one `page.evaluate`.
- `toBeVisible()` on a locator matching two elements fails in strict mode and reports a visibility error for what is really a duplicate-element defect. Count first.
- Two concurrent heavy browser passes caused a `page.goto` timeout that isolated re-measure loads in 4.2 s — don't run two sweeps at once.
- `dashboard` ≠ `forecast`: four routes render one map component with defaultTab=gis; a visible `<h1>` must be overlaid on the canvas, not added inside the map.

## Open, carry-forward (next session)

- `[ASK USER] 1` = publish npm/PyPI packages or stop querying them → closes the last `/download` failure.
- `[ASK USER] 4` = classify the composite-index blocks (keep as pre-publication, or withdraw as derived).
- OP-2 gates: axe + console-error E2E + layout matrix into `verify`.
- 703 undersized touch targets (footer/utility set) — not in OP-1.
- All artifacts left: `/tmp/hn-e2e-5.json`, `/tmp/hn-layout-mobile-final.json`, `/tmp/hn-review-*/`, and the `memory/` doc you are reading.

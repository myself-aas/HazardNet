# Memory — 2026-09-20

HazardNet, branch `arena/01a0bc08-hazardnet`, from commit `28b5fd10`/main.

## What shipped this session (Session 1 — OAuth & Firebase store cutover)

Final revision of the hosted-Postgres-vendor → Firebase migration, made deployable:

- **Prohibited string fully removed** — `git grep -il` (case-insensitive) over the
  tracked tree is empty. Deleted: the cutover-verify workflow (replaced by
  `Firebase-Store-Verify.yml`), the `ssr`/`js` packages of the old vendor
  (frontend `package.json` + lockfiles re-resolved), the seven one-shot codemods
  (`fix.cjs`, `fix_callback.cjs`, `replace_auth.js`, `rewrite_*.cjs`), `bun.lock`
  (duplicate lockfile; `.gitignore` now covers it).
- **Frontend auth** (already Firebase in the prior files) now has zero old-vendor
  config left: `vite.config.ts` `define` block no longer injects any vendor env;
  `components.json` dropped the dangling registry alias.
- **Docs swept** — `docs/oauth-provider-setup.md` rewritten to Firebase Auth only
  (email/password + Google + GitHub); `docs/user-dashboard.md`, `blog-admin-setup.md`,
  ADR 0001 (superseded note), `TARGET_ARCHITECTURE.md`, `PRODUCTION_RUNBOOK.md`,
  `owner-actions.md` and the dated audits all re-anchored to Firebase/self-host
  Postgres-as-analytics.
- **Green-checkpoint bugs found & fixed while re-running the full gates:**
  1. `docs/blog-admin-setup.md` superadmin allowlist now matches the SQL migration
     (`scripts/tests/test_blog_authz_parity.py` was red).
  2. `scripts/build_freshness_artifact.mjs` now emits `generated_at` for the
     site-probe source; `test_status_surface.py` state assertion made SLO-aware.
  3. `.env.example` `FORECAST_STORE=firestore` (was `firebase`, which the validator rejects).
  4. `scripts/security_audit.sh` re-wired to the real CI gates (`npm-audit-ci.mjs`,
     `check-secrets.sh`, `backend/utils/apiKeyAuth.js` path) so it no longer
     false-fails on the public Firebase web key or the timing-safe check.

## Verification (all green)

- `pytest scripts/tests` → **710 passed, 3 skipped** (the Pipeline Scripts Tests
  CI step that was red on `main`).
- Root Jest (backend) → **58/58 suites, 640/640 tests**.
- Frontend Jest → **36/36 suites, 409/409 tests**.
- `npx tsc -p frontend/tsconfig.json --noEmit` → clean; `npm run build` (content
  engine + vite + prerender) → clean; `build_freshness_artifact.mjs --check` → match.
- `node scripts/npm-audit-ci.mjs`, `bash scripts/check-secrets.sh`,
  `node scripts/validate_env.mjs` → all pass.
- `security_audit.sh` passes with production env; `verify-actions-secrets.sh` and
  both workflow YAML files parse (pytest workflow suite covers them).

## Remaining red workflows (owner-only, not repo-flaw)

- Kaggle workflows: 401 from `KAGGLE_USERNAME`/`KAGGLE_KEY` — token rotation at
  kaggle.com → Settings → API. The workflow maps 401 to that exact `::error::`.
- `manual_forecast_ingest.yml`: `data/manual_forecast_*.csv` not uploaded + the
  `BACKEND_API_KEY` GitHub secret missing; preflight names both.
- New `FIREBASE_PROJECT_ID`/`FIREBASE_CLIENT_EMAIL`/`FIREBASE_PRIVATE_KEY` secrets
  must be present for `Firebase-Store-Verify.yml` (owner adds them — secret/variable
  enumeration over CLI is blocked by 403 in this environment).

## Decisions / carry-forward

- Firebase is the **single** database (Auth + Firestore + RTDB). `scripts/db/*.sql`
  remain only as self-host/analytics schemas (ADR 0001-r1 supersession note).
- `FORECAST_STORE=firestore` is the only validator-accepted, documented store mode.
- Remaining open `[ASK USER]` CONCERNS entries (authoritative Firestore database id,
  push-write-path live-ness) stay open for later sessions.
